import { makeStyles } from '@material-ui/core'

export default makeStyles({
    filterBtn: {
        position: 'absolute',
        top: 20,
        right: 20,
        zIndex: 1
    },
    contentMap: {
        position: 'relative'
    }
})